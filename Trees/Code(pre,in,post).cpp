#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*
   1) this is code for pre order traversal
    void preorder(node){
    if(node==null){
    return;
    }
    print(node->data/val);
    preorder(node->left);
    preorder(node->right);
    }
    
2) this is code for inorder traversal

void inorder(node){
if(node==null) return;
inorder(node->left);
print(node->data);
inorder(node->right);
}

3) this is code for post order traversal

void postorder(node){
if(node==null) return;
postorder(node->left);
postorder(node->right);
print(node->data);
}
    */
 return 0;
}