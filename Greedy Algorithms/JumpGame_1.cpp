#include<bits/stdc++.h>
using namespace std;
int main()
{
/*
Jump_game this is a jump game 
in which we have to move the number of spaces on which value we are standing
like arr[]={1 2 3 1 1 0 2 5}
so on 1 we have to move by 1 to 2 and then on 2 we can move by two places 
either on 3 then 1 or directly on second 1 

func(arr){//TC-O(N) , sc-O(1)
max_ind=0;
for(int i=0->n-1){
if(i>max_ind) return false;
max_ind=max(max_index,i+arr[i]);
}
return true;
}
*/
 return 0;
}