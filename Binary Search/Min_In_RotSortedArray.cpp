#include<bits/stdc++.h>
using namespace std;
int main()
{
/*
we have to find min element in rotated sorted array
fun(arr,n){
int ans=INT_MAX;
while(same){
int mid=same;
if(arr[low]<=arr[mid]){
ans=min(ans,arr[low]);
low=mid+1;
}
else {
high=mid-1;
ans=min(ans,arr[mid]);
}
}
return ans;
}
2)optimised approach(don't need an optimisation though )
*/
 return 0;
}
