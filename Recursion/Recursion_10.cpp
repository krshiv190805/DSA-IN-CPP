#include<bits/stdc++.h>
using namespace std;
int main()
{
/*
this is a question of subset sum in which we have to find the sum of 
all the subset of a given array
eg=[3 1 2]
void func(int index,int sum,vector<int>&arr,int N,vector<int>&sumSubset){
if(inde==N);
sumSubset.push_back(sum);
return;
}
//pick the element
func(index+1,sum+arr[index],arr,N,sumSubset);
//Do-not pick the element
func(index+1,sum,arr,N,sumSubset);
}
vector<int>subsetSums(vector<int>arr,int N){
vector<int>sumSubset;
func(0,0,arr,N,sumSubset);
sort(sumSubset.begin(),sumSubset.end());
return sumSubset;
}
total TC-(2^N)*(log(2^N))+2^N
total time complexity is for recursion is two to the power N
and for sorting the array is NlogN where (N<->2^N)

*/
 return 0;
}