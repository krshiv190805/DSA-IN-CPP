#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*
    this is code for pre order traversal
    void preorder(node){
    if(node==null){
    return;
    }
    print(node->data/val);
    preorder(node->left);
    preorder(node->right);
    
    }
    */
 return 0;
}